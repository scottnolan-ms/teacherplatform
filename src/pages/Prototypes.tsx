import { Link } from 'react-router-dom';
// Add new explorations here, in the order they should appear in the hub.
const prototypes=[{title:'Progressive results',path:'/prototypes/progressive-results',description:'Explore results as students work through Custom tasks. Adaptive tasks are coming later.'},{title:'Test controls',path:'/classes/class-test-controls/tasks/linear-equations-test',description:'Explore four task-report layouts, including start, pause, resume, restart and reassignment for groups and individual students.'}];
export default function Prototypes(){return <div className="prototype-page"><h1>Prototypes</h1><p>A place to explore and share ideas for the teacher experience.</p><div className="prototype-list">{[...prototypes].reverse().map(p=><Link className="prototype-card" to={p.path} key={p.path}><div><h2>{p.title}</h2><p>{p.description}</p></div><span>Open prototype →</span></Link>)}</div><footer>More prototypes will be added here as we explore.</footer></div>}

export function ProgressiveResults(){return <div className="prototype-page"><Link to="/prototypes">← Prototypes</Link><h1>Progressive results</h1><p>Explore results before the due date, during the extension window, and after expiry.</p><div className="prototype-list"><Link className="prototype-card" to="/classes/class-progressive-results/tasks/linear-equations-custom"><div><h2>Custom task</h2><p>Live results, student scorecards, question responses and marks.</p></div><span>Open prototype →</span></Link><div className="prototype-card" aria-disabled="true" style={{opacity:.5}}><div><h2>Adaptive task</h2><p>Coming later</p></div><span>Not available yet</span></div></div></div>}
